{
  "name": "solvify",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "mongodb": "^7.1.1"
  }
}
