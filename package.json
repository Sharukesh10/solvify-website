{
  "name": "solvify",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend"
  },
  "dependencies": {
    "mongodb": "^7.1.1"
  }
}
